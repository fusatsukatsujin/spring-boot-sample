version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DYNAMODB_ENDPOINT=http://dynamodb-local:8000
      - AWS_REGION=ap-northeast-1
      - AWS_ACCESS_KEY_ID=dummy
      - AWS_SECRET_ACCESS_KEY=dummy
    depends_on:
      dynamodb-local:
        condition: service_started
      dynamodb-init:
        condition: service_completed_successfully

  dynamodb-local:
    image: amazon/dynamodb-local:latest
    ports:
      - "8000:8000"
    command: "-jar DynamoDBLocal.jar -sharedDb" 

  dynamodb-admin:
    image: aaronshaf/dynamodb-admin
    ports:
      - "8001:8001"
    environment:
      - DYNAMO_ENDPOINT=http://dynamodb-local:8000
    depends_on:
      - dynamodb-local

  dynamodb-init:
    image: amazon/aws-cli
    environment:
      AWS_ACCESS_KEY_ID: dummy
      AWS_SECRET_ACCESS_KEY: dummy
      AWS_DEFAULT_REGION: "ap-northeast-1"
    depends_on:
      - dynamodb-local
    restart: "no"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        echo "Waiting for DynamoDB Local to be ready..."
        until aws dynamodb list-tables --endpoint-url http://dynamodb-local:8000 2>/dev/null; do
          echo "DynamoDB Local is not ready yet. Waiting..."
          sleep 2
        done
        echo "DynamoDB Local is ready. Creating table..."
        aws dynamodb create-table \
          --endpoint-url http://dynamodb-local:8000 \
          --table-name Users \
          --attribute-definitions AttributeName=id,AttributeType=S \
          --key-schema AttributeName=id,KeyType=HASH \
          --provisioned-throughput ReadCapacityUnits=5,WriteCapacityUnits=5 \
          || echo "Table may already exist"
        echo "Table creation completed."